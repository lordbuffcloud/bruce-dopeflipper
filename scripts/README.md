# Scripts

## `generate_logo.py`

Regenerates the Bruce App Store `logo.png` (128×128 RGBA, CK42X teal accent).

Requires Pillow:

```bash
pip install pillow
python scripts/generate_logo.py
```

Output is written to the repository root as `logo.png`.
